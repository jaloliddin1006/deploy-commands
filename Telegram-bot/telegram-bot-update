## cmd da
1. heroku login   // heroku bilan boglanish

2. heroku git:clone -a tatuyordamchibot   // komputerga clone qilib olish

3. cd tatuyordamchibot   // local papkaga kirish

####  lokal papkadagi dasturga o'zgartirishlar kiritiladi ####

4. git add .  // o'zgarishlarni serverga yuklash 

5. git commit -am "make it better"   // serverda "make it better" nomi bilan commit qilish

6. git push heroku master  // o'zgarishlarni saqlagan holda dasturni qayta ishga tushurish
